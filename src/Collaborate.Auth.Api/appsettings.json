{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "Auth": {
    "_comment": "DEV ONLY. A real deployment signs with an asymmetric key held in AWS KMS and never puts key material in config — see ISigningKeyProvider.cs. This is a random 256-bit key, base64-encoded, so the demo/tests can run without any external dependency.",
    "SigningKey": "N2Y5YzFlZDctNTFmMS00MTgxLWEwZjItYzQwYjE1NGJjZjNi",
    "Issuer": "https://auth.collaborate.local"
  }
}
